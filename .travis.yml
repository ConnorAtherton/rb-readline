---

language: ruby

rvm:
  - 2.0.0
  - 2.1.10
  - 2.2.9
  - 2.3.6
  - 2.4.3
  - 2.5.0
  - ruby-head

before_install:
  - gem update --system

script:
  - bundle exec rake

matrix:
  fast_finish: true

  allow_failures:
    - rvm: ruby-head
